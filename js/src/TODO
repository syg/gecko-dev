Parser:
  - remove uses of op on ParseNode

BCE:
  - remove dead StaticScope stuff from SharedContext
  - for loops
  - params
  - TDZ
  - Annex B

VM:
  - Environments:
    - VarEnvironmentObject (CallObject, ModuleEnvironmentObject)
    - LexicalEnvironmentObject
    - WithObject
  - hook new scopes in

XDR
Debugger scopes

rename ScopeCoordinate -> EnvironmentCoordinate
rename BlockScope -> LexicalScope
