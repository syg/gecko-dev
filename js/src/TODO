Parser:
  - remove uses of op on ParseNode

BCE:
  - for loops
  - Annex B

VM:
  - Environments:
    - VarEnvironmentObject (CallObject, ModuleEnvironmentObject)
    - LexicalEnvironmentObject
    - WithObject
  - hook new scopes in
  - use JSOP_PARAMSEND when direct evaling
  - rm nfixed
    - copyRawFrameSlots need to fixed wrt new nfixed() world

XDR
Debugger scopes

rename ScopeCoordinate -> EnvironmentCoordinate
rename BlockScope -> LexicalScope
